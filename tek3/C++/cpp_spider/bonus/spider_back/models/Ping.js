"use strict";

module.exports = (r, models) => {

    const { Ping } = models;

    return {
        /*
            findByMac: function(mac, cb) {
              const user = User.filter({client_mac: mac})
              return user
            }*/
    }
};